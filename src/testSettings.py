scoreFilename ="../score/test.score.mid"
outputDir = "../records/"
splitRecFilename = settings.getSplittedScoreFilename(scoreFilename, outputDir)
recLogFilename = settings.getRecLogFilename(counter,scoreFilename, outputDir) 
outFilename = settings.getOutFilename(counter,scoreFilename, outputDir) 
splittedScoreFilename = settings.getSplittedScoreFilename(counter, scoreFilename, outputDir)
